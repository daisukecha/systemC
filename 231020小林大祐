#include <stdio.h>
int main(void)
{
	// 足し算をする値と答えを格納する箱a,b,cを用意する
	int a, b, c;
	// 足し算をする２つの値を箱aと箱bに格納する
	a = 1;
	b = 2;
	//箱aの値を足し算して、結果を箱cに格納する
	c = a + b
	//箱cの値をディスプレイに表示する
		printf("c = %d\", c);
	return 0;
}
